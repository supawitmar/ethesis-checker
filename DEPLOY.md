# คู่มือติดตั้งและ Deploy — ระบบตรวจรูปเล่ม e-thesis

สรุปขั้นตอนสำหรับนำระบบขึ้นใช้งาน (รันในเครื่อง หรือ deploy บนคลาวด์)

---

## 1. สิ่งที่ต้องมี
- Python 3.10 ขึ้นไป
- แพ็กเกจตาม `requirements.txt` — fastapi, uvicorn, pdfplumber, python-multipart,
  jinja2

> ตั้งแต่นโยบาย ส.ค. 2569 ระบบ **ไม่เทียบชื่อกรรมการ** แล้ว (นับจำนวนอย่างเดียว)
> จึงถอด `pythainlp` และ `onnxruntime` ออก ซึ่งมีไว้ถอดชื่อไทยเป็นอังกฤษเพื่อการเทียบ
> อัปเดตจากเวอร์ชันเก่าให้รัน `pip install -r requirements.txt` ตามปกติ

## 2. ติดตั้ง
```bash
cd code
python -m venv .venv
# Windows PowerShell:
.venv\Scripts\Activate.ps1
# macOS/Linux:
# source .venv/bin/activate
pip install -r requirements.txt
```

## 3. ตั้งค่า Environment Variables

| ตัวแปร | จำเป็น | ค่าเริ่มต้น | ทำอะไร |
|---|---|---|---|
| `APP_PASSWORD` | **ใช่** | (ว่าง) | รหัสผ่านล็อกอินเจ้าหน้าที่ — ถ้าไม่ตั้งจะเข้าใช้ระบบไม่ได้ |
| `MAX_UPLOAD_MB` | ไม่ | 25 | ขนาดไฟล์อัปโหลดสูงสุด |
| `MAX_ACTIVE_JOBS` | ไม่ | 2 | จำนวนงานตรวจพร้อมกันสูงสุด |
| `MAX_KEPT_BOOKS_MB` | ไม่ | 500 | พื้นที่ดิสก์รวมที่เก็บไฟล์รูปเล่มไว้ให้เปิดดูจากหน้ารายงาน เกินงบลบของเล่มเก่าก่อน |
| `COOKIE_SECURE` | ไม่ | ตามโฮสต์ | บังคับคุกกี้เป็น Secure — บน Render เปิดให้อัตโนมัติ โฮสต์ HTTPS อื่นตั้ง `1` เอง |
| `SHEET_WEBHOOK_URL` | ไม่ | (ว่าง) | Web app URL ของ Apps Script ในชีทบันทึกการตรวจ — ไม่ตั้ง ปุ่ม "บันทึกลงชีท" จะไม่ขึ้น |
| `SHEET_WEBHOOK_TOKEN` | ไม่ | (ว่าง) | โทเค็นลับ ต้องตรงกับตัวแปร `TOKEN` ในสคริปต์ |

> **การตรวจเล่มไม่เรียก API ภายนอกเลย** ตรวจด้วยกฎล้วน ๆ ในเครื่อง สองตัวแปรล่างเป็น
> ของปุ่ม "บันทึกลงชีท" ซึ่งไม่บังคับใช้ ไม่ตั้งก็ตรวจเล่มได้ครบทุกอย่างเหมือนเดิม

### ปุ่ม "บันทึกลงชีท" (ไม่บังคับ)

เจ้าหน้าที่กรอกวันที่เข้าคิวกับวันที่ตรวจตั้งแต่หน้าแบบฟอร์ม (ขั้น "วันดำเนินการ" —
ไม่กรอกสองช่องนี้ ระบบไม่ตรวจ) แล้วกดปุ่ม "บันทึกผลการตรวจ" ข้างปุ่มคัดลอกบนหน้ารายงาน
ระบบจะเติมวันที่ตรวจ ผลการพิจารณา และช่องติ๊กจุดผิดลงชีท
"บันทึกการตรวจ E-thesis" ให้เอง ตั้งค่าครั้งเดียวตามหัวข้อใน `tools/sheet_webhook.gs`
(วางสคริปต์ในชีท > Deploy เป็น Web app > เอา URL กับโทเค็นมาใส่สองตัวแปรข้างบน)
**ไม่มีหน้าเว็บเพิ่ม** — Web app ของ Apps Script คือปลายทางที่ระบบเรียกเบื้องหลังเท่านั้น

**กดแล้วขึ้นข้อความแดง** ข้อความบอกสาเหตุมาด้วย เพราะทางแก้คนละทางกันหมด

| ข้อความ | สาเหตุ | แก้ |
|---|---|---|
| ชีทปฏิเสธคำขอ (รหัส 401/403) | สิทธิ์ของ Web app ผิด หรือใช้ URL `/dev` | Deploy ใหม่ให้ Who has access = **Anyone** แล้วใช้ URL ที่ลงท้าย `/exec` |
| ค่า SHEET_WEBHOOK_URL ไม่ใช่ Web app URL … | หยิบผิดช่อง (Deployment ID / ลิงก์หน้าแก้สคริปต์) | คัดลอกช่อง **Web app URL** จาก Deploy > Manage deployments |
| ค่า SHEET_WEBHOOK_URL เป็นลิงก์ทดสอบที่ลงท้าย /dev | ใช้ลิงก์ Test deployment | ใช้ลิงก์ที่ลงท้าย `/exec` |
| ไม่พบสคริปต์ตาม URL (รหัส 404) | ลิงก์ถูกรูปแบบแต่ Google ไม่รู้จัก — ลบ deployment ทิ้ง หรือเป็นลิงก์ของ deployment เก่า | Deploy > Manage deployments แล้วคัดลอก Web app URL อันปัจจุบัน |
| ชีทไม่ตอบภายใน 25 วินาที | สคริปต์ค้าง หรือชีทใหญ่มาก | กดใหม่อีกครั้ง ถ้ายังไม่หายให้ดู Executions ใน Apps Script |
| ชีทตอบกลับมาในรูปแบบที่อ่านไม่ได้ | ได้หน้า HTML แทน JSON = deploy ผิดแบบ หรือสคริปต์พัง | Deploy เป็น **Web app** (ไม่ใช่ Add-on/API executable) แล้วดู Executions |
| ยังไม่ได้ตั้งโทเค็นในสคริปต์ของชีท | วางสคริปต์ใหม่ทับแล้วลืมตั้งโทเค็น | ตั้งที่ Project Settings > Script properties ชื่อ `TOKEN` (ตั้งที่นี่แล้ววางสคริปต์ทับกี่ครั้งก็ไม่หาย) แล้ว Deploy เวอร์ชันใหม่ |
| โทเค็นไม่ตรงกับที่ตั้งไว้ในชีท | `SHEET_WEBHOOK_TOKEN` ไม่ตรงกับ `TOKEN` ในชีท | แก้ให้ตรงกันแล้ว Deploy เวอร์ชันใหม่ |
| ไม่พบแถวของรหัส … | รหัสในชีทกับในฟอร์มไม่ตรง หรืออยู่คนละแท็บ | ตรวจรหัสในแท็บเดือนปัจจุบัน (ระบบเขียนเฉพาะแท็บซ้ายสุด) |
| บันทึกแล้ว … (แท็บนี้ไม่มีช่อง …) | แท็บนั้นไม่มีหัวตารางของช่องที่ต้องติ๊ก | เพิ่มหัวตารางให้ตรงกับแท็บอื่น หรือติ๊กช่องนั้นเอง |

รหัสสาเหตุจะขึ้นใน log ของเซิร์ฟเวอร์ด้วย (บรรทัด `[sheet] …`) โดยไม่มี URL หรือโทเค็น

เปิด Web app URL ในเบราว์เซอร์ได้ทุกเมื่อเพื่อดูสถานะ (อ่านอย่างเดียว ไม่เขียนอะไรลงชีท) —
ตอบเป็น `{"ok":true,"tab":"กย69","token_set":true,"missing_columns":[],...}` · `token_set`
บอกแค่ว่าตั้งโทเค็นไว้แล้วหรือยัง ไม่ได้บอกค่า · `missing_columns` คือช่องติ๊กที่แท็บนั้น
ไม่มีหัวตาราง

> แก้โค้ดในชีทแล้วกด Save เฉย ๆ **URL เดิมยังรันโค้ดเก่า** ต้อง Deploy > Manage
> deployments > แก้ไข > Version: New version ทุกครั้ง (URL ไม่เปลี่ยน)

รันในเครื่อง: คัดลอก `.env.example` เป็น `.env` แล้วใส่ค่า (`.env` ถูก `.gitignore` แล้ว ห้าม commit)

## 4. รันในเครื่อง
```bash
uvicorn main:app --env-file .env --host 0.0.0.0 --port 8000
```
เปิดเบราว์เซอร์ที่ http://localhost:8000 แล้วล็อกอินด้วยรหัสใน `APP_PASSWORD`

## 5. Deploy บนคลาวด์ (เช่น Render)
1. ชี้ Root/Build ไปที่โฟลเดอร์ `code/`
2. Build: `pip install -r requirements.txt`
3. Start: `uvicorn main:app --host 0.0.0.0 --port $PORT --workers 1`
   (แพลตฟอร์มกำหนดพอร์ตผ่าน `$PORT` เอง)
4. ตั้ง Environment Variables ในหน้า **Environment** ของ Render โดยตรง
   (ไม่ต้องใช้ไฟล์ `.env` และห้าม commit คีย์ขึ้น git)

   | Key | Value |
   |---|---|
   | `APP_PASSWORD` | รหัสผ่านเจ้าหน้าที่ (ยาว/สุ่ม) |

   ที่เหลือมีค่าเริ่มต้นอยู่แล้ว ไม่ต้องตั้ง · `COOKIE_SECURE` ไม่ต้องตั้งบน Render
   (ตรวจจาก env `RENDER` ที่แพลตฟอร์มใส่ให้เอง)
5. กด **Manual Deploy → Clear build cache & deploy** เมื่ออัปเดตโค้ดใหม่
6. เช็คว่าขึ้นแล้ว: เปิด `https://<ชื่อบริการ>.onrender.com/health` ต้องได้ `{"status":"ok"}`

> เปลี่ยน `APP_PASSWORD` เมื่อไหร่ เจ้าหน้าที่ทุกคนถูกเตะออกจากระบบทันที (ตั้งใจให้เป็นแบบนี้)
> ส่วนการเพิ่ม/แก้ Environment Variable บน Render จะ restart บริการให้เองอัตโนมัติ

> ⚠️ **ต้องรัน worker เดียว (`--workers 1`)** สถานะงานตรวจเก็บในหน่วยความจำ
> ของโปรเซส ถ้ามีหลาย worker/instance คำขอเช็คสถานะ (`/progress`) อาจไปตกคน
> ละโปรเซสที่ไม่มีงานนั้น ทำให้ขึ้น "ขาดการเชื่อมต่อกับระบบตรวจ" ทั้งที่ตรวจอยู่

## 6. หมายเหตุด้านความปลอดภัย
- คุกกี้ session ตั้ง flag `secure` อัตโนมัติเมื่อรันบน Render (ตรวจจาก env `RENDER`)
  ถ้า deploy บนโฮสต์ HTTPS อื่น ให้ตั้ง `COOKIE_SECURE=1` เอง
  (รันในเครื่องผ่าน http ห้ามตั้ง ไม่งั้นล็อกอินไม่ติด)
- `APP_PASSWORD` เป็นด่านล็อกอินชั้นเดียว — ถ้าเปิดสู่อินเทอร์เน็ต ควรใช้รหัสที่ยาว/สุ่ม
- token ของ session คำนวณจาก `APP_PASSWORD` (HMAC-SHA256) ไม่ได้สุ่มใหม่ทุกครั้ง
  ที่ process เริ่ม เพราะ Render จะ sleep แล้ว cold start เมื่อไม่มีคนใช้สักพัก
  ถ้าสุ่มใหม่ คุกกี้เดิมจะใช้ไม่ได้ทันทีและเจ้าหน้าที่ที่กรอกฟอร์มค้างไว้จะกดตรวจเล่มไม่ได้
  → **เปลี่ยน `APP_PASSWORD` เมื่อไหร่ ทุกคนถูกเตะออกจากระบบทันที** (ตั้งใจให้เป็นแบบนี้)
- ห้าม commit ไฟล์ `.env` หรือคีย์ใด ๆ ขึ้น git
- **ไฟล์รูปเล่มที่ตรวจถูกเก็บบนเซิร์ฟเวอร์ชั่วคราว** (ก.ย. 2569) ให้เจ้าหน้าที่กด
  "📄 เปิดดูไฟล์รูปเล่ม" บนหน้ารายงานได้ — เดิมลบทิ้งทันทีที่ตรวจเสร็จ
  - เก็บในโฟลเดอร์ชั่วคราวของระบบ แยกต่อ process (`<tmp>/ethesis-books/run-xxxx/book-*.pdf`)
    — รันเทสต์หรือเปิดอีก process ขณะเซิร์ฟเวอร์ทำงาน ไม่ไปลบไฟล์ของเซิร์ฟเวอร์ (bug test
    ก.ย. 2569 เจอว่าเดิมไฟล์หาย 2 ใน 3) ปิดตามปกติลบโฟลเดอร์ตัวเอง อายุไฟล์เท่าผลตรวจ
    คือไม่เกิน 12 ชั่วโมง และไม่เกิน 40 เล่มล่าสุด ผลตรวจถูกทิ้งเมื่อไหร่ ไฟล์ถูกลบตาม
  - ตัวกวาดทำงานเองทุก 10 นาที ไม่ต้องรอให้มีคนตรวจเล่มถัดไป และ `/book` กับ `/result`
    ทิ้งของหมดอายุก่อนตอบทุกครั้ง ไฟล์เกิน 12 ชั่วโมงจึงเปิดไม่ได้แน่นอน และหายจากดิสก์
    ภายใน 10 นาทีหลังหมดอายุ (bug test ก.ย. 2569: เดิมทิ้งเฉพาะตอนกดตรวจเล่มใหม่)
  - ไฟล์ที่ไม่มีผลตรวจไหนชี้ถึงแล้วแต่ยังค้างอยู่ (เช่นลบไม่ได้ตอนกำลังถูกเปิดอ่าน) ถูกกวาด
    เมื่อเก่ากว่า 1 ชั่วโมง — ไม่กวาดเร็วกว่านี้ เพราะไฟล์ที่กำลังอัปโหลดยังไม่ผูกกับผลตรวจ
  - รวมกันเกิน `MAX_KEPT_BOOKS_MB` ลบไฟล์ของเล่มเก่าก่อน (รายงานยังเปิดได้ แค่ไม่มีปุ่ม)
  - ตรวจไม่สำเร็จ ลบไฟล์ทันที · restart/cold start ลบไฟล์ค้างทั้งหมด (ผลตรวจหายไปด้วยอยู่แล้ว)
  - เปิดผ่าน `/book/<job_id>` ซึ่งอยู่หลังด่านล็อกอินเหมือนทุกหน้า หาไฟล์จาก job เท่านั้น
    ไม่รับชื่อไฟล์จากคำขอ และตั้ง `Cache-Control: private, no-store` ไม่ให้เบราว์เซอร์แคช
- **`.dockerignore` กัน `.env` ไม่ให้ติดเข้า image** — Dockerfile ใช้ `COPY . .` ถ้าไม่มี
  ไฟล์นี้ การ `docker build` **ในเครื่อง** จะฝัง `.env` (มี `APP_PASSWORD`)
  ลงไปใน layer ซึ่ง **ลบทีหลังไม่ได้** พอ `docker push` ขึ้น
  registry หรือส่ง image ให้คนอื่น คีย์ก็หลุดตามไปด้วย
  (บน Render ไม่เคยเจอปัญหานี้ เพราะ clone จาก git ซึ่งไม่มี `.env` อยู่แล้ว)
  · ไฟล์นี้ยังตัด `tests/` `tools/` `*.md` `.git/` ออกด้วย เหลือเข้า image 12 ไฟล์
  คือ `*.py` + `templates/` + `requirements*.txt` ซึ่งพอสำหรับรันจริงครบทุกกฎ

## 7. ทดสอบว่าระบบทำงาน
```bash
python -m unittest discover -s tests -p "test_*.py"
```
